apiVersion: v1
kind: Service
metadata:
  name: dz02-service
  labels:
    app: dz02
spec:
  selector:
    app: dz02
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 8000
      nodePort: 32000
  type: NodePort
